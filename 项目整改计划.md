# 项目整改计划 — 参赛前行动路线图

## 1. 项目目标与当前评估

**目标**：构建一个 AI 个性化学习平台，支持对话式画像构建、多智能体资源生成、RAG 智能辅导、学习评估与路径规划，以《人工智能导论》为种子课程，达到参赛演示标准。

**当前状态**：核心功能骨架已完成，后端 API 覆盖 16+ 路由模块，前端 12 个页面均已实现，智能体框架（Coordinator / ProfileAgent / ResourceAgent / PathAgent / QAAgent / EvalAgent）完整。并行整改工作（步骤 3-6）已交付代码层面的修复与新增模块，但尚未经过完整的端到端验证和视觉验收。

---

## 2. 已完成事项

以下为本次并行整改已交付的内容：

- **Corrupted UI / Agent 文本修复** — 前端页面中损坏或异常的显示文本已修正
- **凭据清理与 Mock 默认值** — 本地配置中的真实凭据已替换为占位符，`LLM_PROVIDER` 默认使用 `"mock"`
- **旧启动路径修复** — 已识别并修复旧版启动脚本的问题；**但用户明确选择跳过/未完全采纳**新版启动方式，仍以 `start-local.cmd` 为准
- **资源质量审查** — `ResourceReviewer` 模块（`resource_reviewer.py`）实现，支持 6 类资源（document / mindmap / exercise / code / reading / video）的确定性质量评分
- **评估→路径反馈信号** — `EvalAgent._apply_strategies` 产出 `next_actions`，`PathAgent` 消费信号调整难度/节奏/策略，含完整测试覆盖
- **内容安全护栏** — `ContentGuard` 模块实现，覆盖保守的有害输入拒绝、无上下文标记、虚假引用检测
- **/courses /search /settings 页面** — 三个前端页面均已实现并接入后端 API
- **文档修正** — 相关文档已同步更新

---

## 3. 当前验证结果

| 检查项 | 结果 | 备注 |
|--------|------|------|
| 前端构建 | 通过 | `npm run build` 成功 |
| 后端已有测试（test_basic + test_regressions） | 24 项通过 | 含导入检查、JWT、安全、检索策略、SSE、批处理、评估反馈回路等 |
| 内容护栏测试（test_content_guard） | 28 项通过 | prompt 拒绝 8 项 + 正常放行 7 项 + 无上下文 3 项 + 虚假引用 7 项 + wrap 1 项 + GuardDecision 2 项 |
| 资源审查测试（test_resource_reviewer） | 37 项通过 | 通过 `importlib` 隔离加载，绕过重型 `__init__.py` 链 |
| LSP 诊断 | 不可用 | 当前工作区未配置 Language Server |
| 前端分块大小警告 | 存在 | Vite 报告部分 vendor chunk 超过 500 kB，不影响当前构建成功 |

---

## 4. 剩余工作（按优先级排序）

### [P0] 视觉浏览器 QA
- 使用 Playwright 或手动打开每个前端页面，检查布局、渲染、交互是否正常
- 重点：ProfilePage 雷达图、ResourcesPage 分类浏览、EvaluationPage 历史曲线
- 确认无控制台报错、无空白页、无布局断裂

### [P0] 真实端到端流程
- 启动完整环境（`start-local.cmd`），走通一条完整用户旅程：
  登录 → 查看仪表盘 → 对话构建画像 → 查看/生成学习资源 → 提问（QA）→ 查看评估 → 查看学习路径
- 验证 Mock 模式下各 Agent 能正常返回结构化结果

### [P1] 验证内容护栏在 API / 流式场景生效
- 确认 `ContentGuard.check_prompt` 在 chat API 入口处被调用
- 确认 `ContentGuard.check_answer` 在非流式 QA 回答生成后被调用；流式场景当前先验证输入拒绝
- 确认拒绝场景以结构化安全消息返回，而非直接崩溃

### [P1] 验证审查元数据持久化
- 确认 `ResourceReviewer` 的 `review_status` / `review_score` / `review_issues` 字段写入 `learning_resources` 表
- 确认前端资源列表能展示审查状态标记

### [P1] 决定是否移除手动画像编辑
- 正式需求（`require.md` ProfileAgent 约束）明确规定：**"用户不可手动编辑画像维度，只能通过继续对话让 Agent 更新"**
- 当前 ProfilePage 可能仍保留手动编辑入口，需确认并决定：移除编辑 UI，或标记为仅调试可见

### [P2] 清理重复的本地进程 / 端口
- 多次启动可能残留旧进程占用 8000（后端）/ 5173（前端）/ 6333（BGE）端口
- 建议：增强 `stop-local.cmd`，确保 kill 所有相关进程；启动脚本加端口检测

### [P2] 轮换已暴露的外部凭据
- 代码历史中曾出现真实 API Key / 密钥的，需在外部服务（DeepSeek / GLM / 等）控制台重新生成
- 检查 git 历史：`git log --all -p | findstr "sk-"`
- 如果历史中已存在明文凭据，即使当前已删除，也视为已泄露

### [P2] 解决测试环境依赖分裂
- 当前 `test_resource_reviewer.py` 使用 `importlib` 绕过重型依赖链（FlagEmbedding → datasets）
- 长期方案：将纯逻辑模块与重型依赖模块分离，或为 CI 提供轻量测试配置
- 短期：确认 CI 环境能安装所有依赖，或标记需要隔离的测试

---

## 5. 参赛就绪验收标准

满足以下所有条件方可视为"可参赛"：

1. **启动成功** — `start-local.cmd` 一键启动，所有服务健康检查通过
2. **核心流程可走通** — 登录 → 画像 → 资源 → QA → 评估，Mock 模式下无报错
3. **前端无白屏/崩溃** — 12 个页面均可正常渲染，控制台无红色错误
4. **内容护栏生效** — 输入有害内容被拒绝，无上下文回答被标记
5. **凭据安全** — 代码库中无明文真实 API Key，Mock 模式为默认
6. **演示数据可用** — 种子课程《人工智能导论》已入库，可检索出结果
7. **文档完备** — `require.md`（正式需求）、`开源工具清单.md`、`START_TOMORROW.md` 内容准确

---

## 6. 重要说明

**正式需求（`require.md`）是唯一权威来源。** 任何与 `require.md` 不一致的实现决策，以需求文档为准。如果发现矛盾（如手动编辑画像、缺少的路由、不一致的 UI 行为），优先修改代码以匹配需求，而非修改需求以匹配代码。
